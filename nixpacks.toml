[provider]
provider = "debian"

[phases.setup]
    nixpkgsArchive = 'bc7b0a89dbd830397bf834eb38d4e93f1d6a77ca'


[phases.install]
cmds = ['bun install']

[phases.build]
cmds = ['bun run build']

[start]
cmd = 'bun run start'

[variables]
NODE_ENV = 'production'
